<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="addons/settlement_expenses.list">
      <field name="name">addons/settlement_expenses list</field>
      <field name="model">addons/settlement_expenses.addons/settlement_expenses</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="addons/settlement_expenses.action_window">
      <field name="name">addons/settlement_expenses window</field>
      <field name="res_model">addons/settlement_expenses.addons/settlement_expenses</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="addons/settlement_expenses.action_server">
      <field name="name">addons/settlement_expenses server</field>
      <field name="model_id" ref="model_addons/settlement_expenses_addons/settlement_expenses"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="addons/settlement_expenses" id="addons/settlement_expenses.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="addons/settlement_expenses.menu_1" parent="addons/settlement_expenses.menu_root"/>
    <menuitem name="Menu 2" id="addons/settlement_expenses.menu_2" parent="addons/settlement_expenses.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="addons/settlement_expenses.menu_1_list" parent="addons/settlement_expenses.menu_1"
              action="addons/settlement_expenses.action_window"/>
    <menuitem name="Server to list" id="addons/settlement_expenses" parent="addons/settlement_expenses.menu_2"
              action="addons/settlement_expenses.action_server"/>
    -->
  </data>
</odoo>